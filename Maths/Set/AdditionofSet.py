class AdditionofSet:
        def __init__(self):
            self.sizesetA = int(input("Enter Size of Set A"))
            self.sizesetB = int(input("Enter Size of Set A"))
            self.SetSub
            self.SetA = []
            self.SetB = []
            self.SetU = []
            self.setEq = []

            self.value
        def isAvalibale(self):
            i = 0
            while i < self.sizeset:
                if self.SetA[i] == self.value:
                   return False;
                i+=1
            return True
        def isAvalibaleAB(self):
            i=0;
            j =0;
            while self.sizesetA:
                while j < self.sizesetB :
                  if self.SetA[j] == self.SetB[i]:
                    self.setEq[j] = self.SetA[j];
                  j+=1
                slef.SetU[i] = self.SetB[i]
                self.SetSub[i] = self.SetA[i]
                i+=1

Acess = AdditionofSet()
x = isAvalibaleAB()
while True:
    if x == True:
         pass
